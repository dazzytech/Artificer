using UnityEngine;
using System.Collections;
using Data.Space.Library;

/*
namespace Data.Space
{
    [System.Serializable]
    public class WreckageData : SegmentObject
    {
        private ProspectChart prospect;
        
        public WreckageData()
        {
            _type = "asteroid";
        }

        public ProspectChart Prospect
        {
            get { return prospect; }
        }
        
        public void SetProspect()
        {
            prospect = new ProspectChart();

            // only tin and copper 50/50
            prospect.symbol = new string[100];
            int i = 0;
            for(; i < 7; i++)
                prospect.symbol[i] = "Sn";
            for(; i < 14; i++)
                prospect.symbol[i] = "Cu";
            for(; i < 30; i++)
                prospect.symbol[i] = "Fe";
            for(; i < 35; i++)
                prospect.symbol[i] = "Ni";
            for(; i < 45; i++)
                prospect.symbol[i] = "Co";
                    for(; i < 50; i++)
                        prospect.symbol[i] = "Zn";
                    for(; i < 70; i++)
                        prospect.symbol[i] = "Pb";
                    for(; i < 80; i++)
                        prospect.symbol[i] = "Al";
                    for(; i < 87; i++)
                        prospect.symbol[i] = "Ag";
                    for(; i < 95; i++)
                        prospect.symbol[i] = "Au";
                    for(; i < 100; i++)
                        prospect.symbol[i] = "Pt";
        }
    }
}*/

